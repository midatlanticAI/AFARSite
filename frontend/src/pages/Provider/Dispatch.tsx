// @ts-nocheck
import { Typography, Box } from '@mui/material';

export default function DispatchBoard() {
  return (
    <Box>
      <Typography variant="h5" gutterBottom>Dispatch Board</Typography>
      <Typography>Calendar and Kanban board coming soon…</Typography>
    </Box>
  );
} 